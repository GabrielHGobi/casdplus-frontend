import React from "react";
import { View, StyleSheet, Text } from "react-native";

const EssaysScreen = () => {
  return (
    <View>
      <Text>Essays Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default EssaysScreen;
